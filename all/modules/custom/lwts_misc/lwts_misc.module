<?php

/**
 * @file
 * LWtS specific code.
 */

function lwts_misc_node_view_alter( &$build ) {
  if ( ! empty( $build['addthis'] ) ) {
    $build['addthis']['#weight'] = 10;
//  dsm( func_get_args() );
  }
}
